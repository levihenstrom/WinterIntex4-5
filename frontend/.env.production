# Baked in at `npm run build`. Without this, API calls use the frontend origin and /api/* does not exist there.
# No trailing slash. Update if the Azure backend hostname changes.
VITE_API_BASE_URL=https://intex-group4-5-backend-dna7b3ddfvf8dxgb.centralus-01.azurewebsites.net
