/** Default production API host (used when `VITE_API_BASE_URL` is not set at build time). Local dev ignores this; see `src/lib/apiBaseUrl.ts`. */
export const API_URL =
  'https://intex-group4-5-backend-dna7b3ddfvf8dxgb.centralus-01.azurewebsites.net';
